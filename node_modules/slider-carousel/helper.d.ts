/// <reference types="node" />
import { ComponentFactoryResolver, ApplicationRef, Injector, ComponentRef } from '@angular/core';
import { Observable } from 'rxjs';
import { SliderCarouselPreviewComponent } from './slider-carousel-preview/slider-carousel-preview.component';
export declare class Helper {
    private componentFactoryResolver;
    private appRef;
    private injector;
    constructor(componentFactoryResolver: ComponentFactoryResolver, appRef: ApplicationRef, injector: Injector);
    openPreview(data?: {
        [key: string]: any;
    }): {
        onClose: Observable<any>;
        instance: SliderCarouselPreviewComponent;
    };
    createComponent(component: any, data: any, onClose: (data: any) => void): ComponentRef<unknown>;
    smoothScroll(element: HTMLElement, scroll: number, duration?: number, direction?: string): NodeJS.Timer;
    elementIsChild(element: any, parentElment: any): boolean;
    isMobileDevice(): boolean;
}
